import React, { Fragment } from 'react';

const About = () => {
  return (
    <Fragment>
      <p>This app will create a todo list by using hooks in react js.</p>
    </Fragment>
  );
};

export default About;
